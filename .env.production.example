# Production Environment Variables
# This file is committed to Git as a template
# Dokploy will use environment variables from its UI to override these

# API Configuration - Production
# Override this in Dokploy environment variables
NEXT_PUBLIC_API_URL=https://your-production-api-url.com/api/v1

# next-intl configuration
NEXT_INTL_DEFAULT_TIMEZONE=Europe/Amsterdam

# Port Configuration (for Docker)
PORT=4830
HOSTNAME=0.0.0.0

# Stripe (Frontend - Public Key only)
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_xxx
